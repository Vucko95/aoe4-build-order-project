import { type NextPage } from "next";
import Head from "next/head";
import Link from "next/link";
import { api } from "~/utils/api";

const BuildsPage: NextPage = () => {
  const builds = api.builds.getBuilds.useQuery();

  return (
    <>
      <Head>
        <title>AOE4 BUILDS LIST</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center justify-center  gap-4  text-black dark:bg-gray-800 dark:text-white" >
        {/* <h1>Builds</h1> */}
        {/* <Link className="border p-2" href="/"> Home</Link>
        <Link className="border p-2" href="/submit-build"> Submit a new Build</Link> */}

        {/* <table>
            <thead>
                <th>ID</th>
                <th>Civilization</th>
                <th>Build</th>
            </thead>
            <tbody>
                {builds.data?.map((build) => (
                <tr key={build.id}>
                    <td>{build.id}</td>
                    <td>{build.civilization}</td>
                    <td>{build.build}</td>
                </tr>
                   ))  
                }
            </tbody>
        </table> */}
        
<div className="relative overflow-x-auto shadow-md sm:rounded-lg">
    <table className="w-full text-sm text-left text-gray-500 dark:text-gray-400">
        <thead className="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
            <tr>

                <th scope="col" className="px-6 py-3">
                    ID
                </th>
                <th scope="col" className="px-6 py-3">
                    Civilization
                </th>
                <th scope="col" className="px-6 py-3">
                    Build
                </th>

            </tr>
        </thead>
        <tbody>
        {builds.data?.map((build) => (
            <tr key={build.id} className="bg-white border-b dark:bg-gray-900 dark:border-gray-700">
                <th scope="row" className="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
                {build.id}
                </th>
                <td className="px-6 py-4">
                {build.civilization}
                </td>
                <td className="px-6 py-4">
                {build.build}
                </td>
               
            </tr>
                               ))  
                              }
            
        </tbody>
    </table>
</div>

    
      </main>
    </>
  );
};

export default BuildsPage;

